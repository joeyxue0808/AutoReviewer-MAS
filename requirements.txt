# AutoReviewer-MAS Dependencies

# Web framework
fastapi>=0.115.0,<1.0
uvicorn>=0.32.0,<1.0

# LangChain ecosystem
langchain>=1.3.0,<2.0
langchain-openai>=1.2.0,<2.0
langchain-core>=1.4.0,<2.0
langgraph>=1.2.0,<2.0

# Config management
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
pyyaml>=6.0,<7.0

# Retry mechanism
tenacity>=9.0,<10.0

# Docker sandbox
docker>=7.0,<8.0

# VCS clients (using raw aiohttp, not python-gitlab SDK)
aiohttp>=3.10,<4.0

# Message queue (Redis Streams)
redis>=5.0,<9.0

# LangGraph checkpointer (Postgres)
langgraph-checkpoint-postgres>=2.0,<4.0

# Circuit breaker
pybreaker>=1.0,<2.0

# Postgres async driver (for checkpointer)
psycopg[binary]>=3.1,<4.0

# AST analysis (optional, for ast_find_references)
tree-sitter>=0.22,<1.0

# CLI terminal mode
typer>=0.12.0,<1.0
rich>=13.0,<16.0

# ── Testing ──
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=5.0,<8.0
httpx>=0.27,<1.0

# ── Optional: Observability (Langfuse monitoring) ──
# langfuse>=2.0,<5.0

# ── Optional: RAG semantic code search (LanceDB) ──
# pip install lancedb
# lancedb>=0.17

# ── Optional: Local embedding model (requires GPU) ──
# pip install sentence-transformers
# sentence-transformers>=3.0

# ── Optional: Structured logging ──
# pip install structlog
# structlog>=24.0
